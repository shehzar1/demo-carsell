<div class="container">
  <div class="text-center">
    <button type="button" class="btn btn-success btn-lg mt-4">Add Car Information</button>
  </div>
  <div class="progress mt-4 mb-4">
    <div class="progress-bar progress-bar-striped bg-success bar2">Step2</div>
  </div>
  <div class="card text-center">
    <h3>Add Images</h3>
    <%= form_for(@ad, url: wizard_path) do |f| %>
      <%= hidden_field_tag :ad_id, @ad.id %>
      <%= f.file_field :images, multiple: true, direct_upload: true, accept:'image/*', class: "btn btn-outline-success mt-5" %>
      <% if @ad.errors.blank? && @ad.images.attached? %>
        <% @ad.images.each_with_index do |pic| %>
          <%= link_to 'X', ad_step_path(ad_id: @ad.id, picture: pic), method: :delete %>
          <%= image_tag pic, class: "w-25" %>
          <% end %>
        <% end %>
      <% end %>
      <%= f.submit "Add Images", class: "btn btn-primary mt-5 mb-5" %>
    <% end %>
  </div>
</div>
